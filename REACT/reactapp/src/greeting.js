import React from 'react';

const Greeting = ({name}) => {
    <div>
    <h1>This is name format</h1>
    return <h2> hello, {name}!</h2>
    <p>How are you?{name}</p>
    </div>
};

export default Greeting;